# README

## Example of Loading a File
1. Run `racket` on the command line. To start racket in interactive mode.
2. Run `(load "load.scm")` to load the content of load.scm
3. Run `(atom? (quote ()))` to run the function in the file.
